name: Build and deploy Node.js app to Azure Web App - dev-ducky-as

on:
  push:
    branches:
      - Develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read # Required for actions/checkout

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'

      - name: Clean install dependencies
        run: |
          # Clean install to ensure all packages are properly installed
          rm -rf node_modules package-lock.json
          npm cache clean --force
          npm install

      - name: Build application
        run: |
          npm run build --if-present

      - name: Run tests
        run: |
          npm run test --if-present

      - name: List directory to verify build output
        run: ls -alh

      - name: Install production dependencies only
        run: |
          # Clean install production dependencies
          rm -rf node_modules
          npm ci --only=production

      - name: Create deployment package
        run: |
          # Create deployment package with all necessary files
          zip -r release.zip . \
            -x "*.git*" \
            -x "node_modules/.cache/*" \
            -x "*.md" \
            -x ".env*" \
            -x "tests/*" \
            -x "__tests__/*" \
            -x "*.test.js" \
            -x "*.spec.js" \
            -x ".github/*" \
            -x "cypress/*" \
            -x "*.log"

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'dev-ducky-as'
          slot-name: 'Production'
          package: release.zip
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_75BA8F04F8EA4BD3869E7C92764B2217 }}
